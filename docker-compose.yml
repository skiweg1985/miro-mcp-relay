services:
  miro-mcp-relay:
    build: .
    container_name: miro-mcp-relay
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
